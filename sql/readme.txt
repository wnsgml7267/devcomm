이미 'devcomm' 데이터베이스가 생성되어 있다면 'devcomm.sql'을 실행하지 않아도 됩니다.

그리고 나머지 파일을 데이터베이스로 가져옵니다.




If the 'devcomm' database is already created, you do not need to run 'devcomm.sql',

and import the remaining files into the 'devcomm' database.